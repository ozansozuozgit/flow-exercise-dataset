{
  "name": "flow-exercise-dataset",
  "version": "0.1.0",
  "private": true,
  "description": "Free exercise metadata and custom SVG animation frames",
  "type": "module",
  "scripts": { "test": "node scripts/validate.mjs" },
  "engines": { "node": ">=22" }
}
